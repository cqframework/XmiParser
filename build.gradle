apply plugin: 'groovy'

version = '1.0.0'

repositories {
    mavenCentral()
}

dependencies {	
 compile 'org.codehaus.groovy:groovy-all:2.1.6'
 testCompile group: 'junit', name: 'junit', version: '4.11'
}